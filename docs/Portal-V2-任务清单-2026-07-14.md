# Portal V2 任务清单

> 版本目录：`project-mywayaiops-refactor-V2`  
> 创建日期：`2026-07-14`  
> 当前目标：先完成 Portal 入口层重构，聚焦“资源申请工作台 + 服务目录 + 我的申请 + 填写说明”

---

## 一、版本边界

本轮只做 Portal 入口层和首页改造，不深改后续流程页。

已明确范围：

- 左侧导航替换原顶部主导航
- 首页改为“资源申请工作台”
- 原首页服务卡片迁移为“服务目录”
- “平台概览”合并进入“帮助中心 / 填写说明”
- 保留 `我的申请`、`申请详情`、`Apply`、`ApplyService` 现有流程能力

不在本轮范围：

- 不改 Center 结构
- 不改工单状态机
- 不改审批、交付、验收后续链路
- 不做字段全量重构
- 不做正式线上提交闭环

---

## 二、当前已完成

### 任务 1：起 V2 独立版本目录

- 状态：已完成
- 说明：从 `project-mywayaiops-refactor-V1` 复制出 `project-mywayaiops-refactor-V2`
- 价值：保留 V1 作为稳定回滚基线，V2 独立演进

### 任务 2：Portal 主导航改为左侧结构

- 状态：已完成
- 说明：Portal 改为类似 Center 的左侧导航结构
- 当前菜单：
  - `资源申请工作台`
  - `服务目录`
  - `我的申请`
  - `填写说明`
- 底部保留：
  - `帮助中心`

### 任务 3：首页改为资源申请工作台

- 状态：已完成
- 说明：首页不再默认展示服务卡片，改为工作台首屏
- 当前首页包含：
  - 工作台定位说明
  - `向导填写` 模式说明
  - `直接填写` 模式说明
  - 到 `服务目录` 的快捷入口
  - 当前阶段输出与推荐路径说明

### 任务 4：原首页内容迁移为服务目录页

- 状态：已完成
- 说明：原 `HeroSection + ComboCardGrid + AtomicServiceGrid` 已迁移到新 `catalog` 页面
- 当前定位：服务目录作为老手第二入口

### 任务 5：帮助中心内容收敛

- 状态：已完成
- 说明：将“平台概览”口径并入帮助中心，不再作为一级菜单
- 当前帮助中心已补充：
  - Portal V2 当前边界
  - 工作台与服务目录的区别
  - 新用户 / 老用户使用路径

### 任务 6：基础构建校验

- 状态：已完成
- 校验命令：

```bash
npm run build --prefix /Users/gjy/project-mywayaiops-refactor-V2/src/packages/aiops-service-portal
```

- 结果：通过

---

## 三、下一阶段任务

### 任务 7：工作台真实模式切换

- 状态：待开始
- 目标：不是只做首页说明，而是把工作台真正切成两种使用模式
- 子项：
  - 首页内接入 `向导填写`
  - 首页内接入 `直接填写`
  - 两种模式共享草稿状态

### 任务 8：定义工作台字段分步

- 状态：待开始
- 目标：把向导模式拆成明确步骤
- 建议步骤：
  - 基础信息
  - 业务目标
  - 网络与访问
  - SLA / 安全 / 容灾
  - 架构图与附件
  - 预览与导出

### 任务 9：梳理直接填写模式的字段分区

- 状态：待开始
- 目标：基于现有 `ApplyService` 做老手版完整表单
- 子项：
  - 收敛默认展开区块
  - 折叠低频区块
  - 区分推荐字段与用户补充字段

### 任务 10：统一导出目标

- 状态：待开始
- 目标：明确当前阶段导出的材料口径
- 子项：
  - 导出内容结构
  - 附件清单口径
  - 评审摘要口径

### 任务 11：整理页面树与页面职责文档

- 状态：待开始
- 目标：形成可汇报的页面信息架构说明
- 建议内容：
  - 页面名
  - 目标用户
  - 页面职责
  - 本期状态
  - 后续演进方向

---

## 四、汇报建议口径

当前汇报可按下面顺序：

1. 已新起 `V2` 独立版本，确保可随时回滚到 `V1`
2. 已完成 Portal 入口层重构：首页改成“资源申请工作台”
3. 已将原首页下沉为“服务目录”，保留老手使用路径
4. 已将“平台概览”合并进帮助中心，一级菜单更聚焦
5. 下一步进入工作台内部的“向导填写 / 直接填写”真实落地

---

## 五、当前建议的阶段划分

### 阶段 A：入口层重构

- V2 目录隔离
- 左侧导航
- 首页工作台
- 服务目录迁移
- 帮助中心收敛

状态：已完成

### 阶段 B：工作台模式落地

- 向导填写
- 直接填写
- 草稿共享
- 预览与导出入口

状态：待开始

### 阶段 C：字段与导出细化

- 字段分步
- 表单分区
- 评审材料结构
- 导出统一口径

状态：待开始

### 阶段 D：后续流程衔接方案

- 与 `我的申请` 的衔接
- 与正式发起动作的衔接
- 与后续审批/交付链路的衔接

状态：暂不进入开发
