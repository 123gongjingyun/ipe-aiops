import type { AtomicServiceSpec } from "../../types";
import { SLA_PRESETS } from "../sla-presets";
import { STANDARD_FLOW } from "../sla-presets";

const DC_OUTPUT_SCHEMA = [
  { key: "assetId", label: "资产编号", type: "text" as const, required: true },
  { key: "location", label: "位置", type: "text" as const, required: true },
  { key: "status", label: "状态", type: "text" as const, required: true },
];

const DEFAULT_INPUT_SCHEMA = [
  { key: "description", label: "需求描述", type: "textarea" as const, required: true, placeholder: "请描述您的具体需求..." },
  { key: "urgency", label: "紧急程度", type: "select" as const, required: true, options: [{ label: "常规", value: "常规" }, { label: "紧急", value: "紧急" }, { label: "特急", value: "特急" }] },
  { key: "deadline", label: "期望完成时间", type: "text" as const, required: false, placeholder: "如：2026-06-01" },
];

export const dcSpecs: AtomicServiceSpec[] = [
  // 机位管理
  {
    type: "atomic",
    id: "dc-seat-net",
    name: "网络设备上架",
    description: "网络设备位置分配与上架",
    icon: "🏢",
    tags: [],
    version: "1.0.0",
    status: "online",
    domain: "dc",
    category: "机位管理",
    inputSchema: [
      { key: "model", label: "设备型号", type: "text", required: true, placeholder: "如：华为 S5735-L48T4X" },
      { key: "rack", label: "机柜编号", type: "text", required: true, placeholder: "如：A12" },
      { key: "uRange", label: "U位范围", type: "text", required: true, placeholder: "如：U24-U26" },
      { key: "power", label: "电源要求", type: "select", required: true, options: [{ label: "单路", value: "单路" }, { label: "双路", value: "双路" }] },
    ],
    outputSchema: DC_OUTPUT_SCHEMA,
    sla: SLA_PRESETS.bronze,
    flow: STANDARD_FLOW,
    deliveryMode: "manual",
    delivery: { handler: "dc-seat", autoDays: 1, manualDays: 2, dependencies: [] },
  },
  {
    type: "atomic",
    id: "dc-seat-server",
    name: "服务器上架",
    description: "服务器位置分配与上架",
    icon: "🏢",
    tags: [],
    version: "1.0.0",
    status: "online",
    domain: "dc",
    category: "机位管理",
    inputSchema: DEFAULT_INPUT_SCHEMA,
    outputSchema: DC_OUTPUT_SCHEMA,
    sla: SLA_PRESETS.bronze,
    flow: STANDARD_FLOW,
    deliveryMode: "manual",
    delivery: { handler: "default", autoDays: 1, manualDays: 2, dependencies: [] },
  },
  {
    type: "atomic",
    id: "dc-seat-cloud",
    name: "云二期服务器上架",
    description: "云二期服务器位置分配",
    icon: "🏢",
    tags: [],
    version: "1.0.0",
    status: "online",
    domain: "dc",
    category: "机位管理",
    inputSchema: DEFAULT_INPUT_SCHEMA,
    outputSchema: DC_OUTPUT_SCHEMA,
    sla: SLA_PRESETS.bronze,
    flow: STANDARD_FLOW,
    deliveryMode: "manual",
    delivery: { handler: "default", autoDays: 1, manualDays: 2, dependencies: [] },
  },
  {
    type: "atomic",
    id: "dc-seat-storage",
    name: "存储设备上架",
    description: "存储设备位置分配与上架",
    icon: "🏢",
    tags: [],
    version: "1.0.0",
    status: "online",
    domain: "dc",
    category: "机位管理",
    inputSchema: DEFAULT_INPUT_SCHEMA,
    outputSchema: DC_OUTPUT_SCHEMA,
    sla: SLA_PRESETS.bronze,
    flow: STANDARD_FLOW,
    deliveryMode: "manual",
    delivery: { handler: "default", autoDays: 1, manualDays: 2, dependencies: [] },
  },
  // 综合布线
  {
    type: "atomic",
    id: "dc-cabling",
    name: "机房综合布线",
    description: "光纤/网线/电源综合布线方案与实施",
    icon: "🏢",
    tags: [],
    version: "1.0.0",
    status: "online",
    domain: "dc",
    category: "综合布线",
    inputSchema: DEFAULT_INPUT_SCHEMA,
    outputSchema: DC_OUTPUT_SCHEMA,
    sla: SLA_PRESETS.bronze,
    flow: STANDARD_FLOW,
    deliveryMode: "manual",
    delivery: { handler: "default", autoDays: 1, manualDays: 2, dependencies: [] },
  },
];
